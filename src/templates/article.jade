extends layout

block prepend title
  | #{ page.title + " - "}

block content
    article
        h1= page.title

        p.date
            span= moment(page.date).format('MM.DD.YYYY')

        section.content!= typogr(page.html).typogrify()

